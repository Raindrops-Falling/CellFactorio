# CellFactorio
Our first project in Computer Club.

## Running
If this is your first time running the project, make sure to run the setup script first
```sh
./setup.sh
```

To run the web server locally, run this command in the project's root directory
```sh
pygbag .
```
